{
  "private": true,
  "scripts": {
    "preinstall": "cd ledgerjs && yarn install && yarn build",
    "build": "yarn pkg cli"
  },
  "workspaces": [
    "cli",
    "ledgerjs/packages/hw-app-avalanche"
  ],
  "devDependencies": {
    "pkg": "^4.4.9"
  }
}
