{
  "name": "avalanche-wallet-cli",
  "private": true,
	"scripts": {
		"hw-app-avalanche": "cd hw-app-avalanche && yarn install --frozen-lockfile && yarn run build",
		"preinstall": "yarn hw-app-avalanche"
	},
  "workspaces": [
    "hw-app-avalanche",
    "cli"
  ],
  "devDependencies": {
    "pkg": "^4.4.9"
  }
}
