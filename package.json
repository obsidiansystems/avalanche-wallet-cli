{
  "private": true,
  "scripts": {
    "preinstall": "[ \"$(ls -A ledgerjs)\" ] || git submodule update --recursive --init ledgerjs; cd ledgerjs; yarn install --frozen-lockfile && yarn build",
    "build": "yarn pkg cli"
  },
  "workspaces": [
    "cli",
    "ledgerjs/packages/hw-app-avalanche"
  ],
  "devDependencies": {
    "pkg": "^4.4.9"
  }
}
