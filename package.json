{
  "private": true,
  "scripts": {
    "preinstall": "git submodule update --recursive --init && cd ledgerjs && yarn install && yarn build",
    "build": "yarn pkg cli"
  },
  "workspaces": [
    "cli",
    "ledgerjs/packages/hw-app-avalanche"
  ],
  "devDependencies": {
    "pkg": "^4.4.9"
  }
}
