{
  "name": "avalanche-wallet-cli",
  "private": true,
  "scripts": {
    "avalanche": "cd avalanche.js && npm install && npm run build",
    "hw-app-avalanche": "yarn workspace @ledgerhq/hw-app-avalanche install --frozen-lockfile && yarn workspace @ledgerhq/hw-app-avalanche run build",
    "preinstall": "yarn run hw-app-avalanche && yarn run avalanche"
  },
  "workspaces": [
    "avalanche.js",
    "hw-app-avalanche",
    "cli"
  ],
  "devDependencies": {
    "pkg": "^4.4.9"
  }
}
